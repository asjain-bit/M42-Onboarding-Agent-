# Repository Rules

## Git Workflow & Push Restrictions
- **DO NOT push code to GitHub (`git push`) unless the user explicitly asks or instructs you to do so.**
- Local changes, formatting, builds, tests, and local git commits are permitted as part of standard development, but pushing to remote repositories (`git push origin <branch>`) must strictly be explicitly requested by the user first.
