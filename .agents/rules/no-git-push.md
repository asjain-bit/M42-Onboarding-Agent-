# Git Push Restriction Rule

Do not execute `git push` or push code to remote repositories (GitHub, GitLab, Vercel, etc.) unless the user explicitly requests or instructs you to push code in their prompt.
